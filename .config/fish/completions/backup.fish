complete -c backup -s g -l gzip -d "use GZip compression instead of the default BZip2"
complete -c backup -s h -l help -d "output usage information and exit"
complete -c backup -s o -l output -r -d "output file name (default: <directory>.tar.bz2)"
complete -c backup -s V -l version -d "output the version number and exit"
