#! /usr/bin/env bash

isInstalled() {
	hash "$@" 2>/dev/null
}

bkpager() {
	if isInstalled gum; then
		# dimmed border
		gum pager --show-line-numbers=false --border=rounded --border-foreground=#aaa
	else
		less -R
	fi
}

bkpager "$@"
