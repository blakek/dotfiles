#! /usr/bin/env bash

isInstalled() {
	hash "$@" 2>/dev/null
}

bkpager() {
	if isInstalled gum; then
		# dimmed border
		gum pager \
			--border-foreground=#aaa \
			--border=rounded \
			--show-line-numbers=false
	else
		less -R
	fi
}

bkpager "$@"
