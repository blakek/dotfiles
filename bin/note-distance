#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${DOTFILES_ROOT:-}" ]]; then
  echo "Error: DOTFILES_ROOT is not set. Source your dotfiles before running this script." >&2
  exit 1
fi

exec bun run "${DOTFILES_ROOT}/scripts/note-distance/cli.ts" "$@"
